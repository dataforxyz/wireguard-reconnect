#!/bin/bash
# Restart the event monitor after suspend/hibernate. The monitor returns
# immediately, waits for the physical route, and reconnects only when the VPN
# was intended to be on before sleep.

case "${1:-}" in
    post)
        systemctl try-restart wireguard-monitor.service >/dev/null 2>&1 || true
        ;;
esac
